# CLAUDE.md

Read [AGENTS.md](AGENTS.md) for the repository scope, toolchain, commands, test fixtures, conventions, and release guidance. It is the shared source of truth for agent instructions.

Use `build.gradle` for current dependency versions and test environment defaults, `renovate.json` for dependency update policy, and `.github/workflows/` for CI and release behavior.
